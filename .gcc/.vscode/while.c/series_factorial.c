#include<stdio.h>
int main()
{
    int no;
    printf("enter series here\n");
    scanf("%d",&no);
    int fact=1;
    while(no < 1)
    {for()
        fact=fact*(no-1);
        printf("%d\n",fact);
    }
    printf("\n");
    return 0;
}